steps:
  # - name: 'maven'
  #   id: maven-build
  #   args: ["mvn", "clean", "install", "-DskipTest"]

  - name: 'node'
    id: node-build
    args: ["npm", "install"]

  - name: 'node'
    id: node-build
    args: ["npm", "run", "build"]

  - name: 'node'
    id: node-test
    args: ["npm", "run", "test"]

# steps:
# - name: 'gcr.io/cloud-builders/npm' 
# args: ['install']

# - name: 'gcr.io/cloud-builders/npm' 
# args: ['test']

# - name: "gcr.io/cloud-builders/gcloud"
# args: ["app", "deploy"]

# timeout: "1600s"
